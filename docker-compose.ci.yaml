services:
  web:
    container_name: stud-iu
    build: .
    ports:
      - "${PORT:-3000}:3000"
    environment:
      - NEXTAUTH_SECRET=$NEXTAUTH_SECRET
      - DATABASE_URL=$DATABASE_URI
    volumes:
      - ${UPLOADS:-./uploads}:/app/storage
